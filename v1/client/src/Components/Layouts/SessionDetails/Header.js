import React from "react";
import {
  Header,
  Heading,
}
  from "./styledComponents";

const SessionHeader = () => (
  <Header>
    <Heading>
      <h1>Session Details</h1>
    </Heading>
  </Header>
);

export default SessionHeader;
